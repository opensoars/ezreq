const {http, https} = require('reqqer')(['http', 'https']);

/**
 * @return {promise}
 * @example
 * DELETE();
 */
const POST = () => 
  new Promise((resolve, reject) => {

  });

module.exports = POST;
