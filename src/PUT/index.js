const {http, https} = require('reqqer')(['http', 'https']);

/**
 * @return {promise}
 * @example
 * DELETE();
 */
const PUT = () => 
  new Promise((resolve, reject) => {

  });

module.exports = PUT;
